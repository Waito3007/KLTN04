// src/constants/storageKeys.js
// Định nghĩa các key dùng cho localStorage, sessionStorage, ...

export const STORAGE_KEYS = {
  GITHUB_PROFILE: 'github_profile',
  ACCESS_TOKEN: 'access_token',
  // Thêm các key khác nếu cần
};
